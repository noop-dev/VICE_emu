LOAD"TESTx.PRG",8,1
RUN

After some minutes the border gets grey (success) or flashed (failure).

test*n.prg use reference data measured with new CIA (irqX-newcia.dump),
the others use data measured with old CIA (irqX.dump, X = 1..6[b]).

Use record*.prg to measure your own data (stored at $2000-$A000).
